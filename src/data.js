/**
 * Legacy placeholder.
 * Third-party chart data is intentionally not bundled in this app.
 * Use src/data/registry.js for independent public-source datasets.
 */
const CORRELATIONS = [];

export default CORRELATIONS;
